using UnityEngine;

// Runtime tag used by GameBalance to re-apply Inspector tuning to live objects.
public class BalanceIdentity : MonoBehaviour
{
    public GameObject sourcePrefab;
    public bool isEnemy;
}
